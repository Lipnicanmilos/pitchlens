# Core CV / ML
ultralytics>=8.2.0
opencv-python>=4.9.0
# <0.31: sv.ByteTrack je od 0.28 deprecated a v 0.31 ho odstránia.
# Pred odblokovaním stropu treba src/track.py prepísať na boxmot.
supervision>=0.21.0,<0.31
numpy>=1.26.0
pandas>=2.2.0

# Pose estimation (Fáza 4)
# POZOR: mediapipe 1.0 odstránilo legacy `mp.solutions` API, ktoré src/pose.py
# používa. Buď daj sem 'mediapipe<1.0', alebo prepíš pose.py na Tasks API.
mediapipe>=0.10.14

# Tracking (ByteTrack is bundled via supervision/ultralytics;
# boxmot is an alternative with more tracker options)
boxmot>=11.0.0

# Visualization / utils
matplotlib>=3.8.0
pyyaml>=6.0.1
tqdm>=4.66.0

# Optional: for API/backend wrapping later
fastapi>=0.111.0
uvicorn>=0.30.0
